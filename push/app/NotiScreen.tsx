import { SafeAreaView, StyleSheet, Text, View } from "react-native";
import React from "react";

const NotiScreen = () => {
  return (
    <SafeAreaView
      style={{
        flex: 1,
      }}
    >
      <Text style={{ color: "white" }}>NotiScreen</Text>
    </SafeAreaView>
  );
};

export default NotiScreen;

const styles = StyleSheet.create({});
