import { View, Text } from "react-native";
import EmptyState from "../components/EmptyState";

const Home = () => {
  return (
    <View style={{ flex: 1 }}>
      <EmptyState />
    </View>
  );
};

export default Home;
